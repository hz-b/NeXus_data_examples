// docs/js/hypothesis_config.js
window.hypothesisConfig = function () {
  return {
    // Set to true if you want the Hypothesis sidebar to open automatically:
    openSidebar: true,
    // Show existing highlights inline:
    showHighlights: true,
    // Optional: restrict annotations to a specific Hypothesis group (public or private)
    // group: "your-group-name-or-id"
  };
};
